apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bitwarden-eso-provider.fullname" . }}-test-connection"
  labels:
    {{- include "bitwarden-eso-provider.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['curl']
      # see https://bitwarden.com/help/vault-management-api/
      args: ['{{ include "bitwarden-eso-provider.fullname" . }}:{{ .Values.service.port }}/status']
  restartPolicy: Never
